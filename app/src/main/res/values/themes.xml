<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.DisasterBuddy.Splash" parent="Theme.SplashScreen">

        <!-- Background color -->
        <item name="windowSplashScreenBackground">@color/splash_bg</item>

        <!-- Your image -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/app_splash</item>

        <!-- After splash -->
        <item name="postSplashScreenTheme">@style/Theme.DisasterBuddy</item>

    </style>
    <style name="Theme.DisasterBuddy" parent="android:Theme.Material.Light.NoActionBar" />
</resources>